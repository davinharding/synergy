<h1>Chat with <%= @profile.name %>!</h1>

<span id="channel" style="display:none;" data-channel="<%= @channel %>"></span>

<%= react_component 'Messages',
    props: {
      currentProfile: current_user.profile,
      profile:        @profile
    }
%>
